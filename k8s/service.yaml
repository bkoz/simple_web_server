apiVersion: v1
kind: Service
metadata:
  name: simple-web-server
  namespace: default
  labels:
    app: simple-web-server
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: simple-web-server
